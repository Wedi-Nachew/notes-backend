{
    "name": "backend",
    "version": "1.0.0",
    "main": "index.js",
    "scripts": {
        "start": "node index.js",
        "dev": "node --watch index.js pass123",
        "test": "echo \"Error: no test specified\" && exit 1",
        "build:ui": "rm -rf dist && cd ../demo && npm run build && cp -r dist ../backend",
        "deploy": "npm run build:ui && git add . && git commit -m 'updated build' && git push"
    },
    "author": "Filmon Abrha",
    "license": "MIT",
    "description": "",
    "dependencies": {
        "cors": "^2.8.5",
        "dotenv": "^17.0.0",
        "express": "^4.21.2",
        "mongoose": "^8.16.1"
    },
    "devDependencies": {
        "cpy-cli": "^5.0.0",
        "rimraf": "^5.0.5"
    }
}
